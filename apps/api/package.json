{
  "name": "api",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --hot src/app.ts",
    "build": "bun build index.ts --target=node --outdir=dist --minify --sourcemap=external"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.1.5",
    "@scalar/hono-api-reference": "^0.9.26",
    "core": "workspace:*",
    "hono": "^4.10.7",
    "stoker": "^2.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
